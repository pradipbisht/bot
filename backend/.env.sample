PORT=5001
MONGO_URI=
JWT_SECRET=your_secret_key
CORS_ORIGINS=https://bot-three-ruddy.vercel.app
NODE_ENV=production



CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Example Gmail SMTP settings (DO NOT commit real credentials)
# To use Gmail, enable 2FA on the account and create an App Password, then set:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=pradipbisht007@gmail.com
SMTP_PASS=glxt iazf oiof sfvp
CONTACT_RECEIVER=pradipbisht007@gmail.com

# Note: Using a Gmail App Password is required (regular account password will not work
# if 2FA is enabled). For production use prefer a transactional provider (SendGrid,
# Mailgun, SES) for better deliverability.

# npm i googleapis
# Add env vars you must set:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5001/api/auth/google/callback
FRONTEND_URL=http://localhost:5173

# Optional: default admin created at startup if no admin exists.
# Set these to auto-create an admin user (do NOT commit real credentials)
# DEFAULT_ADMIN_EMAIL=admin@example.com
# DEFAULT_ADMIN_PASSWORD=SuperSecureP@ssw0rd
# DEFAULT_ADMIN_NAME=Site Admin
